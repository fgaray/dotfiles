auto-reload yes
reload-time 60
cleanup-on-quit yes
confirm-exit yes
max-items 100
refresh-on-startup yes
browser "firefox -new-tab %u --display=:0.0"

#Nos va a decir cuando llegen nuevos items
notify-screen yes
notify-xterm yes

# vim-like navigation
bind-key j down
bind-key k up
bind-key h left
bind-key l right 
bind-key m toggle-show-read-feeds
